import React from 'react';

export default function Wishlist () {
  return (
    <></>
    /*
    <div className="p-4 bg-white rounded-lg shadow-md">
      <h3 className="text-xl font-semibold mb-4">Mi Wishlist</h3>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        {products.map(product => (
          <div key={product.productid} className="p-4 bg-gray-100 rounded-lg flex flex-col items-center">
            <img src={product.imageurls? product.imageurls[0] : ""} alt={product.name} className="w-24 h-24 mb-4"/>
            <h4 className="text-lg font-semibold mb-2">{product.name}</h4>
            <p className="text-gray-600 mb-2">{product.price.toFixed(2)}</p>
            <button className="px-4 py-2 bg-green-500 text-white rounded">Agregar al carrito</button>
          </div>
        ))}
      </div>
    </div>
    */
  );
};
